#!/usr/bin/node

// script that prints 3 lines by using an array of string and a loop

const favs = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

favs.forEach(function (fav) {
  console.log(fav);
});
